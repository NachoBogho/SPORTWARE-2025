{
  "name": "sportware-backend",
  "version": "1.0.0",
  "description": "Backend para SportWare",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node scripts/seedDatabase.js seed",
    "seed:clean": "node scripts/seedDatabase.js seed --clean",
    "db:check": "node scripts/seedDatabase.js check",
    "db:clean": "node scripts/seedDatabase.js clean",
    "system:check": "node scripts/checkSystem.js",
    "port:kill": "node scripts/portManager.js kill 3001",
    "port:kill-all": "node scripts/portManager.js kill-all",
    "port:check": "node scripts/portManager.js check",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.0.1",
    "morgan": "^1.10.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}