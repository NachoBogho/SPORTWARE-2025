{
  "name": "sportware",
  "version": "1.0.0",
  "description": "Sistema de gestión de canchas deportivas",
  "main": "main.js",
  "scripts": {
  "start": "electron .",
  "dev": "concurrently \"npm run dev:react\" \"npm run dev:electron\"",
  "dev:react": "cd frontend && npm run dev",
  "dev:electron": "nodemon --exec electron .",
  "dev-all": "concurrently -k -r \"cd backend && npm run dev\" \"cd frontend && npm run dev\" \"wait-on tcp:5173 && cd desktop && npm run dev:electron\"",
  "build": "npm run build:react && npm run build:electron",
  "build:react": "cd frontend && npm run build",
  "build:electron": "electron-builder",
  "postinstall": "cd frontend && npm install && cd ../backend && npm install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "electron": "^28.0.0",
    "electron-builder": "^24.6.0",
    "nodemon": "^3.0.1"
  },
  "dependencies": {},
  "build": {
    "appId": "com.sportware.app",
    "productName": "SportWare",
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}